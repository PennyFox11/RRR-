using UnityEngine;

public class Diamond_collection : MonoBehaviour
{
    public Diamond_collection()
    {
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
