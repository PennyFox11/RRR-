using UnityEngine;

public class PlayerMove : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("player standing still"); // Print 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
